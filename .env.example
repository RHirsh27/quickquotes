# Quotd Environment Variables
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# REQUIRED - Core Supabase Configuration
# =============================================================================
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# =============================================================================
# REQUIRED - Stripe Configuration (Payment Processing)
# =============================================================================
# Get these from Stripe Dashboard: https://dashboard.stripe.com/apikeys
# IMPORTANT: Use test keys for development, live keys for production
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key-here
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key-here

# Stripe Webhook Secret (from Stripe Dashboard > Developers > Webhooks)
# Create webhook endpoint: https://yourdomain.com/api/stripe/webhook
# Required events: checkout.session.completed, customer.subscription.updated,
#                  customer.subscription.deleted, account.updated
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret-here

# Stripe Price IDs (from Stripe Dashboard > Products)
# Create these products first, then copy the price IDs here
# IMPORTANT: Also update src/config/pricing.ts with these same IDs
NEXT_PUBLIC_STRIPE_PRICE_SOLO=price_your-solo-plan-price-id
NEXT_PUBLIC_STRIPE_PRICE_CREW=price_your-crew-plan-price-id
NEXT_PUBLIC_STRIPE_PRICE_TEAM=price_your-team-plan-price-id

# Stripe Extra Seat Price ID (for Team plan overage billing)
# Create a recurring product for additional seats at $25/month
NEXT_PUBLIC_STRIPE_EXTRA_SEAT_PRICE_ID=price_your-extra-seat-price-id

# =============================================================================
# REQUIRED - Application URL
# =============================================================================
# Your production domain (used for redirect URLs, emails, etc.)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# REQUIRED - Email Service (Resend)
# =============================================================================
# Get your API key from: https://resend.com/api-keys
# Used for transactional emails (payment receipts, invoices, notifications)
RESEND_API_KEY=re_your-resend-api-key-here

# =============================================================================
# OPTIONAL - Admin Configuration
# =============================================================================
# Email address for receiving feedback and admin notifications
ADMIN_EMAIL=admin@yourdomain.com

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
#
# 1. NEVER commit .env.local or .env to version control
# 2. Use different Stripe keys for development vs production
# 3. Set NEXT_PUBLIC_APP_URL to your actual domain in production
# 4. Verify Stripe webhook endpoint is registered in Stripe Dashboard
# 5. Test the full checkout flow before going live
#
# Vercel Deployment:
# - Set all environment variables in Vercel Dashboard
# - Ensure STRIPE_WEBHOOK_SECRET matches your production webhook
# - Use Stripe live keys (not test keys) in production
#
# Security:
# - Keep STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET secure
# - Never expose these in client-side code
# - Rotate keys if compromised
#
